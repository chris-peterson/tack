---
description: Run the tack CLI directly (route tracker for AI-assisted development)
argument-hint: "[install-cli [--dir <path>] | <any tack subcommand>]"
---

Run the bundled tack CLI with the user's arguments. With no arguments, prints help. The shim builds the CLI on first run.

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/tack" $ARGUMENTS
```
