name: Check

# Delegates to shipyard's reusable check gate — verifies the committed
# plugin.json and plugin.yml suite.describe still match their source.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  check:
    uses: chris-peterson/shipyard/.github/workflows/check.yml@main
