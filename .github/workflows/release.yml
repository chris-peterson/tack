name: Release

# Delegates to shipyard's reusable release workflow (float to @actions during
# consolidation). secrets: inherit passes MARKETPLACE_DISPATCH_TOKEN through.

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    uses: chris-peterson/shipyard/.github/workflows/release.yml@main
    secrets: inherit
